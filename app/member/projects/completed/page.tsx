import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { PageHeader, SimpleTable, StatCard } from "@/components/member/member-ui";
import { completedProjects, formatCurrency } from "@/lib/member-mock-data";
export default function Page() { const total = completedProjects.reduce((s, p) => s + p.myDistribution, 0); return <div><PageHeader title="Completed Projects"><p>View completed project outcomes, final cost examples, sale information, and member-specific mock distribution summaries.</p></PageHeader><div className="mb-6 grid gap-4 md:grid-cols-3"><StatCard label="Completed projects" value={`${completedProjects.length}`} note="Prototype closed projects" /><StatCard label="My distributions" value={formatCurrency(total)} note="Member-specific total" /><StatCard label="Final statuses" value="Distributed" note="All records are mock data" /></div><div className="space-y-5">{completedProjects.map((project) => <Card key={project.id}><CardHeader><CardTitle>{project.title}</CardTitle></CardHeader><CardContent className="space-y-4"><p className="text-sm text-muted-foreground">{project.summary}</p><SimpleTable headers={["Completed","Final status","Total cost","Sale price","My units","My contribution","My distribution"]} rows={[[project.completedAt, project.finalStatus, formatCurrency(project.totalCost), formatCurrency(project.salePrice), project.myUnits, formatCurrency(project.myContribution), formatCurrency(project.myDistribution)]]} /></CardContent></Card>)}</div></div>; }
