import Link from "next/link";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { PageHeader, ProgressBar } from "@/components/member/member-ui";
import { activeProjects } from "@/lib/member-mock-data";
export default function Page() { return <div><PageHeader title="Active Projects"><p>Track active projects tied to confirmed or submitted mock participations. Lifecycle progress and manager labels are prototype-only.</p></PageHeader><div className="grid gap-5 lg:grid-cols-2">{activeProjects.map((project) => <Card key={project.id}><CardHeader><div className="flex items-center justify-between"><CardTitle>{project.title}</CardTitle><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">{project.status}</span></div></CardHeader><CardContent className="space-y-4"><ProgressBar value={project.progressPercent} /><div className="grid gap-3 text-sm md:grid-cols-2"><p><span className="text-muted-foreground">Started:</span> {project.startDate}</p><p><span className="text-muted-foreground">Current phase:</span> {project.currentPhase}</p><p><span className="text-muted-foreground">Next milestone:</span> {project.nextMilestone}</p><p><span className="text-muted-foreground">Manager:</span> {project.managerId}</p></div><p className="rounded-2xl bg-slate-50 p-4 text-sm text-muted-foreground">{project.latestUpdate}</p><Link href="/member/opportunities" className="text-sm font-semibold text-emerald-700">Open related opportunity</Link></CardContent></Card>)}</div></div>; }
