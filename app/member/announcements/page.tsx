import Link from "next/link";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge, PageHeader } from "@/components/member/member-ui";
import { announcements, opportunities } from "@/lib/member-mock-data";
export default function Page() { return <div><PageHeader title="Announcements"><p>Published announcements for the member audience, including priority labels, related opportunities, and expiry dates.</p></PageHeader><div className="grid gap-5">{announcements.map((item) => { const opp = opportunities.find((o) => o.id === item.relatedOpportunityId); return <Card key={item.id}><CardHeader><div className="flex flex-wrap items-center gap-2"><Badge>{item.priority}</Badge><Badge>{item.status}</Badge><span className="text-sm text-muted-foreground">Published {item.publishDate}</span></div><CardTitle>{item.title}</CardTitle></CardHeader><CardContent><p className="font-medium">{item.summary}</p><p className="mt-3 text-sm text-muted-foreground">{item.body}</p><div className="mt-4 flex flex-wrap gap-4 text-sm"><span>Expires {item.expiryDate}</span>{opp && <Link className="font-semibold text-emerald-700" href={`/member/opportunities/${opp.slug}`}>Related: {opp.title}</Link>}</div></CardContent></Card>; })}</div></div>; }
